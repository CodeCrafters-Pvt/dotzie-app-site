---
// Placeholder production page.
//
// This branch (`launch`) exists only to occupy the Production environment while
// the real site is kept private. Vercel's Hobby plan cannot protect a production
// domain, and it refuses to track a branch that doesn't exist -- so production
// serves this blank page instead of the marketing site.
//
// The real site lives on `dev` and deploys as a protected preview.
// Do NOT merge this branch into `dev`. See LAUNCH-CHECKLIST.md.
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="robots" content="noindex, nofollow" />
    <title>&nbsp;</title>
  </head>
  <body></body>
</html>
